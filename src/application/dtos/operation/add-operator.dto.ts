export interface AddOperatorInput {
  operationId: string;
  sellerId: string;
  operatorId: string;
}

export interface AddOperatorOutput {
  operationId: string;
  sellerId: string;
  operatorId: string;
}